const TopBar = () => {
    return (
        <div>
            <button className="topBarButton">Ticker</button>
            <button className="topBarButton">Price</button>
            <button className="topBarButton">Market Cap</button>
            <button className="topBarButton">Volume</button>
            <button className="topBarButton">Beta</button>
        </div>
    )
}

export default TopBar